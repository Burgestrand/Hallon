source :rubygems
gemspec

# Useful for testing out audio.
unless ENV["TRAVIS"] # does not build on travis
  gem 'hallon-openal'
end

gem 'ruby_parser', '>= 3.0.0.a1', '< 4.0'
gem 'pry'
gem 'simplecov'
gem 'rb-readline'
